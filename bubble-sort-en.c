// Bubble Sort é um algoritmo de ordenação onde a ideia é percorrer o vetor diversas vezes, e a cada passagem fazer flutuar para o topo o maior elemento da sequência.
// Este arquivo contém uma implementação usando recurssão e usando loops.
// Bubble Sort is a sorting algorithm. The basic ideia is to go through a vector various times and each time move the biggest element to the end of the vector.
// This file has the recurssive implementation and the implementation using loops.

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

// Intervalo de valores indo de 0 - 500 que irão compor o vetor
// Range of values 0 - 500 that will compose an 1D array
#define MAX 500

void 

int main(){
    int n;

    printf("Escolha o tamanho do vetor que voce deseja ordenar: ");
    //printf("Choose the lenght of the array you wants to sort: ");
    scanf("%d", &n);
    printf("\nGerando valores aleatorios para um vetor de %d posicoes...\n", n);
    //printf("\nGenerating random values for an array of %d positions...\n", n);
    
    return 0;
}
